# ── Web framework ──────────────────────────────────────────
flask==3.0.3
flask-socketio==5.3.6
python-socketio==5.11.3
python-engineio==4.9.1
gunicorn==22.0.0
eventlet==0.36.1

# ── OpenCV — SADECE headless (libGL gerektirmez) ───────────
# NOT: ultralytics opencv-python (full) çekmeye çalışır.
# railway.toml install fazı bunu pip uninstall ile temizler.
opencv-python-headless==4.9.0.80

# ── Sayısal / görüntü ──────────────────────────────────────
numpy==1.26.4
Pillow==10.3.0

# ── YOLO (opencv-python bağımlılığı railway.toml'da yönetilir) ──
ultralytics==8.2.18

# ── PDF ────────────────────────────────────────────────────
reportlab==4.2.0
