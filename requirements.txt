# Web framework
flask==3.0.3
flask-socketio==5.3.6
python-socketio==5.11.3
python-engineio==4.9.1
gunicorn==22.0.0
eventlet==0.36.1

# OpenCV — headless only
opencv-python-headless==4.9.0.80

# Numerical / image
numpy==1.26.4
Pillow==10.3.0

# YOLO dependencies — manuel olarak listelendi
# ultralytics direkt yazılmıyor çünkü opencv-python (full) çekiyor
# Bunun yerine ultralytics'in tüm bağımlılıkları burada:
torch
torchvision
matplotlib
pandas
psutil
pyyaml
requests
scipy
seaborn
tqdm
lapx>=0.5.2

# PDF
reportlab==4.2.0
